---
apiVersion: gateway.networking.k8s.io/v1beta1
kind: Gateway
metadata:
  name: color-gw
  namespace: handson-cilium
spec:
  gatewayClassName: cilium
  listeners:
  - protocol: HTTP
    port: 80
    name: web-gw-color
    allowedRoutes:
      namespaces:
        from: Same
---
apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: color-route-1
  namespace: handson-cilium
spec:
  parentRefs:
  - name: color-gw
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /color
    backendRefs:
    - kind: Service
      name: handson-blue
      port: 80
      weight: 50
    - kind: Service
      name: handson-yellow
      port: 80
      weight: 50
