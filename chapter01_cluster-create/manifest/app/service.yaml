---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: handson
  name: handson
spec:
  type: ClusterIP
  ports:
  - name: http-app
    port: 8080
    protocol: TCP
    targetPort: http
  selector:
    app: handson
